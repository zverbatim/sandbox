/**
 * Static import makes the code more clean,
 * yet possible name conflict
 */

import static Calendar.getInstance as now
import static java.lang.Math.*

println now().timeInMillis
println sin(0)
println sqrt(16)